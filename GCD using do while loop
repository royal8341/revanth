#include <iostream>
using namespace std;

int main() {
    int p, q;
    cin >> p >> q;
    while (p != q) {
        if (p > q) {
            p -= q;
        } else {
            q -= p;
        }
    }

    cout << "\nThe GCD number is: " << p << "\n";
    return 0;
}
